import React from 'react'

const PetInfo = ({
  petId,
  _id,
  species,
  colors,
  age,
  gender,
  size,
  coat,
  name,
  photo
}) => {
  return (
    <div>{name}</div>
  )
}

export default PetInfo;